# Exclude Vim's swap files
**/*.swp

# Exclude plug.vim
vim/vim/autoload/plug.vim
vim/vim/.*
vim/vim/undo/*
vim/vim/plugged/*

# kitty.conf is dynamically generated from `setup` utility
kitty/.config/kitty/kitty.conf

# fish "universal" variables are stored in a file that
# dynamically includes the hostname of local machine.
# Ignore all "fishd.*" files except for "fishd.HOSTNAME"
fish/.config/fish/fishd.*
!fish/.config/fish/fishd.HOSTNAME

dotdrop.sh
